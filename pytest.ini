[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    unit: fast isolated tests with no external I/O
    integration: hit real MongoDB / Redis
    e2e: full stack via FastAPI TestClient
